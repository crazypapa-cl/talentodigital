
    var fecha =new  Date(),
        hora = fecha.getHours(),
        minutos = fecha.getMinutes(),
        segundo = fecha.getSeconds()
        console.log('Hora:, '+hora+' Minutos:, '+minutos+' Segundos:, '+segundo)

        
